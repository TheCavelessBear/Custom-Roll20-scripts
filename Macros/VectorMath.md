# VectorMath reference

Active source: `Scripts/VectorMath.js`.  
Complexity: simple command script (library-only).  
Direct Roll20 commands: none. Selection is not read and there is no macro entry point.

For integration context only, the script publishes the JavaScript `VecMath` geometry API. It supplies vector operations used by geometry and lighting scripts. JavaScript calls to `VecMath` are not Roll20 chat macros.

Registry/help reconciliation: the active source and registry agree that no event or chat entry point exists.
