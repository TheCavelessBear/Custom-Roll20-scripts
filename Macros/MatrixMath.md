# MatrixMath reference

Active source: `Scripts/MatrixMath.js`.  
Complexity: simple command script (library-only).  
Direct Roll20 commands: none. Selection is not read and there is no macro entry point.

For integration context only, the script publishes the JavaScript `MatrixMath` transformation API used by PathMath and other geometry code. It is not callable as a Roll20 macro.

Registry/help reconciliation: the active source and registry agree that no chat command exists.
